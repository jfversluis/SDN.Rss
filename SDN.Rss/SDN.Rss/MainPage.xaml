<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SDN.Rss.MainPage">
  <ContentPage.Padding>
    <OnPlatform x:TypeArguments="Thickness"
                iOS="0, 20, 0, 0"
                Android="0, 0, 0, 0"
                WinPhone="0, 0, 0, 0" />
  </ContentPage.Padding>

  <StackLayout>
    <SearchBar Placeholder="Zoeken..." TextChanged="SearchBar_OnTextChanged"></SearchBar>
    <ListView x:Name="RssList" ItemsSource="{Binding RssNodes}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding Title}" Detail="{Binding Summary}" />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
</ContentPage>